#include "site.h"
#include<iostream>
#include<vector>
#include<stack>
using namespace std;
int main() {
	site s;
	
	return 0;
}
	/*vector<messages>i;
	messages m;
	m.id = 10;
	m.message = "mona";
	i.push_back(m);
	for (int n = 0; n < 6; n++)
	{
		m.id += 1;
		i.push_back(m);
	}
	for (int n = 0; n < i.size(); n++)
	{
		cout << i.at(n).id << "\n";
	}
	int index;
	cout << "enter the index you want to remove: ";
	cin >> index;
	i.erase(i.begin() + index);
	for (int n = 0; n < i.size(); n++)
	{
		cout << i.at(n).id << "\n";
	}*/









	
	/*user u;
	u.display();*/